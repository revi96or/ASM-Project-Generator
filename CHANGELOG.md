<!--
Описание: Журнал изменений проекта ASM Project Generator
Версия: 1.0.0
Автор: Новожилов Артем
-->

# CHANGELOG

## 3.1.30

- В `Info` добавлена цветовая легенда из макроса `AddColorLegendToInfo` и показана в предпросмотре P&P.

## 3.1.29

- В модалках `Принять` и `Да` кнопки снова разделены; сдвиг убран.

## 3.1.28

- В диалогах `Принять` и `Да` кнопки слегка сдвинуты влево.

## 3.1.27

- В жёлтой подсветке предпросмотра `SETxx` цвет текста теперь контрастный.

## 3.1.26

- В предпросмотре таблицы P&P в программе подсвечивается жёлтым автозаполненный столбец `SETxx`.

## 3.1.25

- В `SETxx` теперь подсветка светло-жёлтым `RGB(255, 255, 200)` только после ответа `Да`.

## 3.1.24

- Встроено Excel-подобное окно для проверки пустого `SETxx` с кнопками `Да` и `Нет`.
- При подтверждении пустой столбец `SETxx` заполняется и `xlsx` пересобирается.

## 3.1.23

- Добавлено встроенное окно для выбора исполнения платы `0-10` с записью в `Info!D3`.
- После подтверждения исполнение прокидывается в состояние P&P и в пересоздаваемый `xlsx`.

## 3.1.22

- Для `FormatCSVTable` в P&P теперь верхний регистр заголовков, автоширина столбцов и левое выравнивание диапазона.
- Превью `ImportedCSVTable` в программе приведено к виду сформированного листа.

## 3.1.21

- Лист `Info` в программе теперь показывает реальные ячейки `D5` и `D6`, как в создаваемом `xlsx`.

## 3.1.20

- В импортируемых строках сохранён текстовый формат для ключевых P&P-колонок.
- Для `Rotation` и служебных счётчиков применён числовой формат.
- `QueryTables.Add` и `QueryTable` в без-Excel режиме помечены как пропущенные, а их результат реализован собственным парсером и `xlsx`-генератором.

## 3.1.19

- Подправлен XML таблицы под формат, который ожидает Excel.
- Связь таблицы на листе теперь идёт на `/xl/tables/table1.xml`, а лист получает стандартные `pageMargins`.

## 3.1.18

- Лист `Лист1` теперь получает реальную таблицу Excel `ImportedCSVTable`.
- В xlsx добавлены таблица `table1.xml` и связи worksheet/table.

## 3.1.17

- Лист `Info` теперь заполняет только `D5` и `D6`.
- Поля `A` и `B` на листе `Info` больше не используются.

## 3.1.16

- После импорта CSV книга `baseName.xlsx` создаётся сразу в папке `Экспорт XLSX`.
- P&P-импорт теперь отдаёт путь созданного xlsx вместе с предпросмотром и статистикой.

## 3.1.15

- Коммит `6420dde`: стартовая P&P-шапка больше не перетирается проверкой принтера.
- В режиме `Расстановщик` в шапке сохраняются заглушки, пока набор плашек не утвержден.

## 3.1.14

- Коммит `b3c317d`: стартовая шапка P&P теперь подгружается вместе с правой зоной.
- На старте для `Расстановщик` показываются P&P-данные вместо принтерных.

## 3.1.12

- Коммит `e32d226`: при запуске активной становится вкладка `Расстановщик`.
- Стартовый фокус панели навигации теперь соответствует первому P&P-разделу.

## 3.1.11

- Коммит `f41a894`: P&P-пути теперь восстанавливаются после перезапуска программы.
- Добавлен отдельный блок `pnpPaths` в пользовательские настройки.
- Поля P&P сохраняются вместе с обычными путями принтера, АОИ и расстановщика.

## 3.1.9

- Коммит `1a23cb5`: после сохранения P&P-путей показывается сообщение.
- Для P&P-полей добавлен такой же диалог обновления каталога, как и для обычных путей.

## 3.1.8

- Коммит `2e33cf9`: P&P-state теперь сохраняет и восстанавливает все важные пути.
- Добавлены/синхронизированы `localPath`, `importCsvPath`, `exportXlsxFolder`, `exportFolder`, `dictPath` и `statePath`.
- Ручной ввод в полях путей теперь не теряется при save/load.

## 3.1.7

- Коммит `baae8df`: P&P теперь создаёт `baseName.xlsx` без Excel.
- В xlsx сохраняются только листы `Лист1` и `Info`.
- Путь вывода берётся из поля `Экспорт XLSX`.
- Шаг `Создаёт новую книгу Excel...` отмечен в `TASKS.md` как выполненный.

## 3.1.6

- Коммит `870be95`: добавлено поле `Экспорт XLSX` в блоке `Расстановщик`.
- Синхронизированы версии в `package.json`, `package-lock.json`, `main.js` и renderer.
- Поле `Экспорт XLSX` теперь сохраняется и восстанавливается вместе с P&P-state.

## 3.1.5

- Коммит `2dacd06`: зафиксирована текущая точка P&P-подсистемы.
- Добавлена кнопка `Отмена` в блоке `Расстановщик`.
- Добавлен общий флаг отмены для P&P-операций.
- Отмена теперь сбрасывает P&P-контекст к стартовому состоянию.
- Обновлён `TASKS.md` с отметкой частичной реализации отмены.

## 3.1.4

- Добавлен повторный запуск импорта CSV через кнопку `Создать проект P&P`.
- Исправлена логика повторного выбора CSV при уже открытой таблице.
- Версии синхронизированы в `package.json`, `main.js`, `package-lock.json` и renderer.

## 3.1.3

- Добавлена базовая связка P&P с импортом CSV, предпросмотром и экспортом.
- Добавлен `TASKS.md` для пошагового ведения переноса макросов.
