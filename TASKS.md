<!--
Описание: Список задач проекта ASM Project Generator
Версия: 1.1.2
Автор: Новожилов Артем
-->

# TASKS

1. Работаем над перенесением логики макроса `ImportCSVAsTableFromFolder7_6`.

- [~] Проверяет флаг отмены и сразу выходит, если операция уже отменена. (частично реализовано в 3.1.5 через кнопку `Отмена`, общий флаг отмены и проверки на границах операции; системный file dialog не прерывается мгновенно)
- [x] Открывает диалог выбора CSV-файла. (реализовано в 3.1.4)
- [x] Берёт полный путь, выделяет имя файла, убирает расширение и префикс `Pick Place for `. (реализовано в 3.1.4)
- [x] Создаёт новую книгу Excel и сохраняет её как `baseName.xlsx`. (реализовано в 3.1.7 как xlsx-экспорт без Excel в папку из `Экспорт XLSX`)
- [x] Перед импортом переводит нужные столбцы в текстовый формат: `COMMENT`, `FOOTPRINT`, `Center-X`, `Center-Y` и/или `Designator`. (реализовано в 3.1.20 через текстовые стили и сохранение значений по ключевым колонкам)
- [x] Импортирует CSV через `QueryTables.Add` на лист `A1`. (в без-Excel режиме пропущено, но результат реализован собственным парсером и генератором `xlsx`)
- [x] Явно задаёт типы столбцов при импорте, чтобы текст не превратился в числа. (реализовано в 3.1.20 через нормализацию строк и стили ячеек)
- [x] Удаляет временный `QueryTable`. (в без-Excel режиме `QueryTable` не создаётся)
- [x] Создаёт лист `Info`. (реализовано в 3.1.7 внутри xlsx-экспорта)
- [x] Копирует `A10` исходного листа в `Info!D5`. (реализовано в 3.1.7 внутри xlsx-экспорта)
- [x] Записывает полный путь к CSV в `Info!D6`. (реализовано в 3.1.7 внутри xlsx-экспорта)
- [x] Возвращается на первый лист. (реализовано в 3.1.7: первым листом в книге идёт `Лист1`)
- [x] Удаляет первые 12 строк. (реализовано в 3.1.4)
- [x] После удаления снова ищет в первой строке столбцы `Center-X(mm)` и `Center-Y(mm)`. (реализовано в 3.1.4)
- [x] Если эти столбцы не найдены, показывает `MsgBox` со списком заголовков и завершает макрос. (реализовано в 3.1.4 как ошибка импорта с текстом заголовков)
- [x] Если найдены — переводит эти столбцы в текстовый формат. (реализовано в 3.1.20 через текстовые стили и сохранение значений без потери вида)
- [x] Определяет последнюю строку и последний столбец. (реализовано в 3.1.4)
- [x] Создаёт умную таблицу `ListObject` из всего диапазона данных. (реализовано в 3.1.18 как `xl/tables/table1.xml` в создаваемом xlsx)
- [x] Даёт таблице имя `ImportedCSVTable`. (реализовано в 3.1.18 как имя таблицы, а не листа)
- [x] Завершает работу. (реализовано в 3.1.4)

Закрыли перенос первого макроса 11.08.2026 19:45
Коммит: `ad9a68b` (`Render Info sheet preview`).

2. Работаем над перенесением логики макроса FormatCSVTable

- [x] Берет активный лист текущей книги и работает только с ним. (реализовано в 3.1.22 для `Лист1`/`ImportedCSVTable`)
- [x] Определяет последнюю заполненную строку и последний заполненный столбец. (реализовано в 3.1.22 при расчёте диапазона и ширин)
- [x] Преобразует заголовки первой строки в верхний регистр. (реализовано в 3.1.22 в `xlsx` и превью)
- [x] Автоматически подбирает ширину всех столбцов по содержимому. (реализовано в 3.1.22 в создаваемом `xlsx`)
- [x] Выравнивает весь использованный диапазон по левому краю. (реализовано в 3.1.22 через стили ячеек)
- [x] Завершает работу без дополнительных окон и сохранений. (реализовано в 3.1.22)

Закрыли перенос второго макроса 11.08.2026 19:58
Коммит: `5600571` (`Implement FormatCSVTable`).

3. Работаем над перенесением логики макроса AskBoardVersion и юзерформы Execution

- [x] Проверяет флаг отмены перед открытием формы и сразу выходит, если операция уже отменена. (встроено через общий P&P-cancel перед запуском шага и повторный выход по `Отмена`)
- [x] Открывает форму с вопросом `Какое исполнение платы? [0-10]` и полем ввода числа. (реализовано во встроенном модальном окне программы)
- [x] Показывает подсказку к полю: `Введите число от 0 до 10. Полной плате соответствует 0`. (реализовано в модальном окне)
- [x] Принимает значения `0`–`10`, где `01`, `1`, `02`, `2` и т.д. считаются одинаковыми. (реализовано в нормализации ввода)
- [x] По кнопке подтверждения записывает число в `Info!D3`. (реализовано в P&P-состоянии, превью и пересоздаваемом xlsx)
- [x] По кнопке отмены или закрытию формы прерывает цепочку макросов. (отмена возвращает `null` и шаг прекращается)
- [ ] После успешного подтверждения продолжает выполнение следующего шага макросов.

Закрыли перенос третьего макроса и юзерформы 11.08.2026 20:48
Коммит создан: `679e4dd` — `Implement P&P execution prompt`.

4. Работаем над перенесением логики макроса CheckAndFillSetColumn

- [~] Перед стартом проверяет флаг отмены и сразу выходит, если операция уже отменена. (частично: отказ в диалоге и общий `Cancel` уже работают, но отдельный preflight перед открытием шага ещё можно добавить)
- [x] Берёт `Info!D3` и формирует имя столбца `SET00` … `SET10` с ведущим нулём. (реализовано в приложении при разборе ввода)
- [x] На листе `Лист1` ищет столбец `SETxx` в первой строке. (реализовано в проверке таблицы импорта)
- [x] Если нужный столбец не найден, завершает шаг без заполнения. (реализовано как тихий выход)
- [x] Определяет последнюю заполненную строку по первому столбцу. (реализовано при анализе `SETxx`)
- [x] Проверяет, есть ли в найденном столбце значения `1` или `R`. (реализовано перед показом вопроса)
- [x] Если значений нет, показывает вопрос о заполнении всех строк базовым исполнением. (реализовано в Excel-подобном окне)
- [x] При отказе вызывает отмену операции и прерывает цепочку макросов. (реализовано через `Отмена`)
- [x] При подтверждении заполняет все непустые строки значением `1` в столбце `SETxx`. (реализовано через заполнение данных и пересборку `xlsx`)
- [x] Подсвечивает автозаполненные ячейки светло-жёлтым цветом. (реализовано в `xlsx` для заполненного после `Да` столбца `SETxx`)
- [x] Завершает шаг без дополнительных окон. (после ответа `Да/Нет` шаг заканчивается)
