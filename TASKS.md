<!--
Описание: Список задач проекта ASM Project Generator
Версия: 1.0.0
Автор: Новожилов Артем
-->

# TASKS

Работаем над перенесением логики макроса `ImportCSVAsTableFromFolder7_6`.

- [~] Проверяет флаг отмены и сразу выходит, если операция уже отменена. (частично реализовано в 3.1.5 через кнопку `Отмена`, общий флаг отмены и проверки на границах операции; системный file dialog не прерывается мгновенно)
- [x] Открывает диалог выбора CSV-файла. (реализовано в 3.1.4)
- [x] Берёт полный путь, выделяет имя файла, убирает расширение и префикс `Pick Place for `. (реализовано в 3.1.4)
- [x] Создаёт новую книгу Excel и сохраняет её как `baseName.xlsx`. (реализовано в 3.1.7 как xlsx-экспорт без Excel в папку из `Экспорт XLSX`)
- [~] Перед импортом переводит нужные столбцы в текстовый формат: `COMMENT`, `FOOTPRINT`, `Center-X`, `Center-Y` и/или `Designator`. (частично реализовано в 3.1.4 через хранение значений как текста)
- [ ] Импортирует CSV через `QueryTables.Add` на лист `A1`.
- [~] Явно задаёт типы столбцов при импорте, чтобы текст не превратился в числа. (частично реализовано в 3.1.4 через нормализацию при парсинге CSV)
- [ ] Удаляет временный `QueryTable`.
- [x] Создаёт лист `Info`. (реализовано в 3.1.7 внутри xlsx-экспорта)
- [x] Копирует `A10` исходного листа в `Info!D5`. (реализовано в 3.1.7 внутри xlsx-экспорта)
- [x] Записывает полный путь к CSV в `Info!D6`. (реализовано в 3.1.7 внутри xlsx-экспорта)
- [x] Возвращается на первый лист. (реализовано в 3.1.7: первым листом в книге идёт `Лист1`)
- [x] Удаляет первые 12 строк. (реализовано в 3.1.4)
- [x] После удаления снова ищет в первой строке столбцы `Center-X(mm)` и `Center-Y(mm)`. (реализовано в 3.1.4)
- [x] Если эти столбцы не найдены, показывает `MsgBox` со списком заголовков и завершает макрос. (реализовано в 3.1.4 как ошибка импорта с текстом заголовков)
- [~] Если найдены — переводит эти столбцы в текстовый формат. (частично реализовано в 3.1.4 через строковое хранение координат)
- [x] Определяет последнюю строку и последний столбец. (реализовано в 3.1.4)
- [ ] Создаёт умную таблицу `ListObject` из всего диапазона данных.
- [ ] Даёт таблице имя `ImportedCSVTable`.
- [x] Завершает работу. (реализовано в 3.1.4)