<!--
Описание: Список задач проекта ASM Project Generator
Версия: 1.1.2
Автор: Новожилов Артем
-->

# TASKS

1. Работаем над перенесением логики макроса `ImportCSVAsTableFromFolder7_6`.

- [~] Проверяет флаг отмены и сразу выходит, если операция уже отменена. (частично реализовано в 3.1.5 через кнопку `Отмена`, общий флаг отмены и проверки на границах операции; системный file dialog не прерывается мгновенно)
- [x] Открывает диалог выбора CSV-файла. (реализовано в 3.1.4)
- [x] Берёт полный путь, выделяет имя файла, убирает расширение и префикс `Pick Place for `. (реализовано в 3.1.4)
- [x] Создаёт новую книгу Excel и сохраняет её как `baseName.xlsx`. (реализовано в 3.1.7 как xlsx-экспорт без Excel в папку из `Экспорт XLSX`)
- [x] Перед импортом переводит нужные столбцы в текстовый формат: `COMMENT`, `FOOTPRINT`, `Center-X`, `Center-Y` и/или `Designator`. (реализовано в 3.1.20 через текстовые стили и сохранение значений по ключевым колонкам)
- [x] Импортирует CSV через `QueryTables.Add` на лист `A1`. (в без-Excel режиме пропущено, но результат реализован собственным парсером и генератором `xlsx`)
- [x] Явно задаёт типы столбцов при импорте, чтобы текст не превратился в числа. (реализовано в 3.1.20 через нормализацию строк и стили ячеек)
- [x] Удаляет временный `QueryTable`. (в без-Excel режиме `QueryTable` не создаётся)
- [x] Создаёт лист `Info`. (реализовано в 3.1.7 внутри xlsx-экспорта)
- [x] Копирует `A10` исходного листа в `Info!D5`. (реализовано в 3.1.7 внутри xlsx-экспорта)
- [x] Записывает полный путь к CSV в `Info!D6`. (реализовано в 3.1.7 внутри xlsx-экспорта)
- [x] Возвращается на первый лист. (реализовано в 3.1.7: первым листом в книге идёт `Лист1`)
- [x] Удаляет первые 12 строк. (реализовано в 3.1.4)
- [x] После удаления снова ищет в первой строке столбцы `Center-X(mm)` и `Center-Y(mm)`. (реализовано в 3.1.4)
- [x] Если эти столбцы не найдены, показывает `MsgBox` со списком заголовков и завершает макрос. (реализовано в 3.1.4 как ошибка импорта с текстом заголовков)
- [x] Если найдены — переводит эти столбцы в текстовый формат. (реализовано в 3.1.20 через текстовые стили и сохранение значений без потери вида)
- [x] Определяет последнюю строку и последний столбец. (реализовано в 3.1.4)
- [x] Создаёт умную таблицу `ListObject` из всего диапазона данных. (реализовано в 3.1.18 как `xl/tables/table1.xml` в создаваемом xlsx)
- [x] Даёт таблице имя `ImportedCSVTable`. (реализовано в 3.1.18 как имя таблицы, а не листа)
- [x] Завершает работу. (реализовано в 3.1.4)

Закрыли перенос первого макроса 11.08.2026 19:45
Коммит: `ad9a68b` (`Render Info sheet preview`).

2. Работаем над перенесением логики макроса FormatCSVTable

- [x] Берет активный лист текущей книги и работает только с ним. (реализовано в 3.1.22 для `Лист1`/`ImportedCSVTable`)
- [x] Определяет последнюю заполненную строку и последний заполненный столбец. (реализовано в 3.1.22 при расчёте диапазона и ширин)
- [x] Преобразует заголовки первой строки в верхний регистр. (реализовано в 3.1.22 в `xlsx` и превью)
- [x] Автоматически подбирает ширину всех столбцов по содержимому. (реализовано в 3.1.22 в создаваемом `xlsx`)
- [x] Выравнивает весь использованный диапазон по левому краю. (реализовано в 3.1.22 через стили ячеек)
- [x] Завершает работу без дополнительных окон и сохранений. (реализовано в 3.1.22)

Закрыли перенос второго макроса 11.08.2026 19:58
Коммит: `5600571` (`Implement FormatCSVTable`).

3. Работаем над перенесением логики макроса AskBoardVersion и юзерформы Execution

- [x] Проверяет флаг отмены перед открытием формы и сразу выходит, если операция уже отменена. (встроено через общий P&P-cancel перед запуском шага и повторный выход по `Отмена`)
- [x] Открывает форму с вопросом `Какое исполнение платы? [0-10]` и полем ввода числа. (реализовано во встроенном модальном окне программы)
- [x] Показывает подсказку к полю: `Введите число от 0 до 10. Полной плате соответствует 0`. (реализовано в модальном окне)
- [x] Принимает значения `0`–`10`, где `01`, `1`, `02`, `2` и т.д. считаются одинаковыми. (реализовано в нормализации ввода)
- [x] По кнопке подтверждения записывает число в `Info!D3`. (реализовано в P&P-состоянии, превью и пересоздаваемом xlsx)
- [x] По кнопке отмены или закрытию формы прерывает цепочку макросов. (отмена возвращает `null` и шаг прекращается)
- [ ] После успешного подтверждения продолжает выполнение следующего шага макросов.

Закрыли перенос третьего макроса и юзерформы 11.08.2026 20:48
Коммит создан: `679e4dd` — `Implement P&P execution prompt`.

4. Работаем над перенесением логики макроса CheckAndFillSetColumn

- [~] Перед стартом проверяет флаг отмены и сразу выходит, если операция уже отменена. (частично: отказ в диалоге и общий `Cancel` уже работают, но отдельный preflight перед открытием шага ещё можно добавить)
- [x] Берёт `Info!D3` и формирует имя столбца `SET00` … `SET10` с ведущим нулём. (реализовано в приложении при разборе ввода)
- [x] На листе `Лист1` ищет столбец `SETxx` в первой строке. (реализовано в проверке таблицы импорта)
- [x] Если нужный столбец не найден, завершает шаг без заполнения. (реализовано как тихий выход)
- [x] Определяет последнюю заполненную строку по первому столбцу. (реализовано при анализе `SETxx`)
- [x] Проверяет, есть ли в найденном столбце значения `1` или `R`. (реализовано перед показом вопроса)
- [x] Если значений нет, показывает вопрос о заполнении всех строк базовым исполнением. (реализовано в Excel-подобном окне)
- [x] При отказе вызывает отмену операции и прерывает цепочку макросов. (реализовано через `Отмена`)
- [x] При подтверждении заполняет все непустые строки значением `1` в столбце `SETxx`. (реализовано через заполнение данных и пересборку `xlsx`)
- [x] Подсвечивает автозаполненные ячейки светло-жёлтым цветом. (реализовано в `xlsx` для заполненного после `Да` столбца `SETxx`)
- [x] Завершает шаг без дополнительных окон. (после ответа `Да/Нет` шаг заканчивается)

Закрыли перенос четвертого макроса 11.08.2026 22:02 (в D3 Info цифра меняется на 01, 02..., т.е. не 1, 2... после ответа да о заполнении SET. Оставил так, надеюсь не помешает)
Коммит создан: Коммит создан: `499ed0c` — **Refine P&P dialogs and preview**.

5. Работаем над перенесением логики макроса AddColorLegendToInfo

- [x] Берет лист `Info` из активной книги и использует его как единственный источник легенды.
- [x] Заполняет `A10`/`B10` светло-розовым цветом и текстом про данные, которые заменились из словаря.
- [x] Заполняет `A11`/`B11` светло-зеленым цветом и текстом про частичное совпадение в словаре с пометкой проверить красный цвет.
- [x] Заполняет `A12`/`B12` красным цветом и текстом про данные, которые не совпадают с данными в словаре.
- [x] Заполняет `A13`/`B13` оранжевым цветом и текстом про русские буквы в столбце `COMMENT`.
- [x] Заполняет `A14`/`B14` желтым цветом и текстом про замены `COMMENT` на `R_COMMENT`, `DESIGNATOR` на `R_DESIGNATOR` или `TOL` на `R_TOL` при наличии `R` в `SET`.
- [x] Заполняет `A15`/`B15` светло-желтым цветом и текстом про автозаполнение `SET` значением `1` после ответа `Да`.
- [x] Завершает работу без дополнительных диалогов и без изменения других листов.

Закрыли перенос пятого макроса 15.08.2026 15:24
Коммит создан: `0efd0b0` — **Add P&P info legend**.

6. Работаем над перенесением логики макроса CopySheet1AsDataSet

- [x] Берет первый лист активной книги как источник данных.
- [x] Копирует первый лист целиком в конец книги, сохраняя содержимое и оформление.
- [x] Берет только что созданную копию как новый лист.
- [x] Переименовывает новый лист в `DataSet`.
- [x] Завершает работу без дополнительных действий и окон.

Закрыли перенос шестого макроса 15.08.2026 15:41
Коммит создан: `08fcec6` — **Fix DataSet table XML**.

7. Работаем над перенесением логики макроса DeletePCBRows

- [x] Ищет лист `DataSet` в активной книге и завершает шаг с ошибкой, если лист не найден.
- [x] Ищет столбец `COMMENT` в первой строке листа `DataSet`.
- [x] Завершает шаг с ошибкой, если столбец `COMMENT` не найден.
- [x] Определяет последнюю заполненную строку по столбцу `COMMENT`.
- [x] Завершает шаг с информацией, если данных для обработки нет.
- [x] Отключает обновление экрана и ручной пересчет на время удаления.
- [x] Проходит строки снизу вверх, начиная со второй строки.
- [x] Удаляет строки, где значение в `COMMENT` начинается с `Плата печатная` без учета регистра.
- [x] Считает количество удаленных строк.
- [x] Восстанавливает пересчет и обновление экрана после завершения.
- [x] При ошибке показывает сообщение с номером и описанием ошибки и тоже восстанавливает настройки Excel.

Закрыли перенос седьмого макроса 15.08.2026 15:54
Коммит создан: `0252230` — **Implement DeletePCBRows in P&P**.

8. Работаем над перенесением логики макроса `CopyRowsWithREFtoDataSet9` и функций `FilterSetColumnByD3_2` и связанный макрос `DeleteNotFittedRows`.

- [x] `CopyRowsWithREFtoDataSet9`: берет `Лист1` как источник и `DataSet` как приемник.
- [x] `CopyRowsWithREFtoDataSet9`: проверяет наличие умной таблицы на `DataSet`.
- [x] `CopyRowsWithREFtoDataSet9`: ищет столбец `DESIGNATOR` на `Лист1`.
- [x] `CopyRowsWithREFtoDataSet9`: собирает все строки с `REF*` из `Лист1` в список исходных REF.
- [x] `CopyRowsWithREFtoDataSet9`: завершает работу, если на `Лист1` вообще нет `REF`.
- [x] `CopyRowsWithREFtoDataSet9`: ищет столбец `DESIGNATOR` на `DataSet`.
- [x] `CopyRowsWithREFtoDataSet9`: ищет столбец `VARIATION` на `DataSet`.
- [x] `CopyRowsWithREFtoDataSet9`: находит все столбцы `SET*` на `DataSet`.
- [x] `CopyRowsWithREFtoDataSet9`: проходит существующие строки `DataSet`, запоминает уже имеющиеся `REF` и ставит им `1` во все `SET*`.
- [x] `CopyRowsWithREFtoDataSet9`: для уже найденных `REF` также ставит `VARIATION = Fitted`.
- [x] `CopyRowsWithREFtoDataSet9`: определяет какие `REF` с `Лист1` еще отсутствуют в `DataSet`.
- [x] `CopyRowsWithREFtoDataSet9`: если новых `REF` нет, завершает шаг без добавления строк.
- [x] `CopyRowsWithREFtoDataSet9`: добавляет новые `REF` в `DataSet` через таблицу или дописывание строки.
- [x] `CopyRowsWithREFtoDataSet9`: для новых строк ставит `1` во все `SET*`.
- [x] `CopyRowsWithREFtoDataSet9`: для новых строк ставит `VARIATION = Fitted`.
- [x] `CopyRowsWithREFtoDataSet9`: обновляет счетчик добавленных `REF` и статистику `SET`.
- [x] `FilterSetColumnByD3_2`: берет значение `Info!D3` и строит имя столбца `SET00`…`SET10`.
- [x] `FilterSetColumnByD3_2`: ищет этот столбец в первой строке листа `DataSet2/Лист3`.
- [x] `FilterSetColumnByD3_2`: если столбец не найден, возвращает `0`.
- [x] `FilterSetColumnByD3_2`: определяет последнюю заполненную строку по найденному столбцу `SET`.
- [x] `FilterSetColumnByD3_2`: снимает старые фильтры.
- [x] `FilterSetColumnByD3_2`: включает автофильтр и оставляет только значения `1` и `R` в нужном `SET`.
- [x] `FilterSetColumnByD3_2`: возвращает номер найденного столбца `SET`.
- [x] `DeleteNotFittedRows`: принимает номер столбца `SET` и сразу выходит, если он равен `0`.
- [x] `DeleteNotFittedRows`: берет лист `DataSet2/Лист3` как рабочий.
- [x] `DeleteNotFittedRows`: находит столбцы `VARIATION` и `DESIGNATOR`.
- [x] `DeleteNotFittedRows`: инициализирует коллекцию удаленных компонентов для статистики.
- [x] `DeleteNotFittedRows`: отключает обновление экрана на время удаления.
- [x] `DeleteNotFittedRows`: идет снизу вверх только по видимым строкам.
- [x] `DeleteNotFittedRows`: удаляет строки, где `SET=1` и `VARIATION=Not Fitted`.
- [x] `DeleteNotFittedRows`: сохраняет `DESIGNATOR` удаленных строк в коллекцию.
- [x] `DeleteNotFittedRows`: увеличивает глобальный счетчик удаленных строк.
- [x] `DeleteNotFittedRows`: включает обновление экрана обратно и завершает работу.

Закрыли перенос восьмого макроса 15.08.2026 18:35
Коммит `0a65caa` — `Mark P&P task 8 complete`. Говорит что перенос не 1-в-1, типа реально столбцы не сортируются по Set=0 и SET=1 и R, но это не помешает дальнейшей логике

9. Работаем над перенесением логики макроса `ReplaceCommentAndDesignatorForR_Highlight3`

- [x] Берет `Info!D3` и строит имя столбца `SET00`…`SET10`.
- [x] Ищет этот столбец в `Лист3` / `DataSet2`.
- [x] Определяет последнюю строку по столбцу `COMMENT`.
- [x] Находит столбцы `COMMENT`, `DESIGNATOR` и `TOL`.
- [x] Собирает все `R_COMMENT*`, `R_DESIGNATOR*` и `R_TOL*` вместе с их числовыми индексами.
- [x] Проходит только видимые строки, где в нужном `SET` стоит `R`.
- [x] Для каждой такой строки сначала ищет замену с тем же индексом, потом fallback без индекса.
- [x] Заменяет `COMMENT`, `DESIGNATOR` и `TOL` найденными значениями из `R_*`.
- [x] Подсвечивает замененные ячейки желтым.
- [x] Не делает замену, если в колонке `SET` нет строк со значением `R`.

Закрыли перенос девятого макроса 15.08.2026 18:35
Коммит `0d62596` (`Close P&P task 9`) (отображает реальные строки, без скрытых. Excel отображает слева счетчик вместе со скрытыми, потому кажется что есть разница)

10. Работаем над перенесением логики макроса `CopySheetDataSetToDataSet2` 

- [x] Берет 3-й лист активной книги как источник.
- [x] Копирует этот лист в конец книги.
- [x] Переименовывает копию в `DataSet2`.

Закрыли перенос десятого макроса 15.08.2026 20:26.
Коммит: `600e717` — `Add DataSet2 sheet copy`

11. Работаем над перенесением логики макроса `ReplaceRuToEn`

- [x] Открывает лист `DataSet2`.
- [x] Ищет в первой строке столбец `COMMENT`.
- [x] Если `COMMENT` не найден, показывает сообщение и выходит.
- [x] Определяет последнюю заполненную строку по `COMMENT`.
- [x] Проходит строки со 2-й до последней.
- [x] Заменяет `Гц` на `Hz`, `мк` на `u`, `Гн` на `H`.
- [x] Заменяет отдельные буквы `М`, `к`, `К`, `В`, `х`, `Г`, `н` на латиницу.
- [x] Записывает обновлённый текст обратно в `COMMENT`.

Закрыли перенос логики одинадцатого макроса 15.08.2026 20:40.
Коммит: `beafbfd` — `Add ReplaceRuToEn to P&P`

12. Работаем над перенесением логики макроса `HighlightRussianLettersInComment` 

- [x] Берет активный лист `DataSet2` как рабочий.
- [x] Ищет в первой строке столбец `COMMENT`.
- [x] Если `COMMENT` не найден, показывает сообщение и выходит.
- [x] Определяет последнюю заполненную строку по `COMMENT`.
- [x] Создает regex для русских букв `[А-Яа-яЁё]`.
- [x] Проходит строки со 2-й до последней.
- [x] Подсвечивает ячейки `COMMENT` с русскими буквами оранжевым.

Закрыли перенос логики двенадцатого макроса 15.08.2026 20:54.
Коммит: `b567c83` (`Close P&P task 12`).

13. Работаем над перенесением логики макроса CleanTOLAndRemoveSpacesSafely3 / ReplaceCommaWithDotInCommentAndFootprint_Safe / CopySheetDataSet2TotResist

- [x] Активный лист для шага — `DataSet2`.
- [x] Очищает `TOL`, убирает пробелы из текстовых ячеек, кроме `CENTER-X(MM)` и `CENTER-Y(MM)`.
- [x] Заменяет запятые на точки в `COMMENT` и `FOOTPRINT`.
- [x] Копирует `DataSet2` в `DataResist`.

Закрыли перенос тринадцатого, четырнадцатого и пятнадцатого макроса 15.08.2026 22:06.
Коммит `6eb7b6c` — `Close P&P task 13`.

14. Работаем над перенесением логики макроса ProcessResistors (FULL_START_TO_2)

- [x] Открывает `Dict.xlsx` и находит лист `DataResist` в активной книге.
- [x] Находит лист `Resist` в словаре и проверяет, что оба листа существуют.
- [x] Находит в `DataResist` столбцы `COMMENT`, `FOOTPRINT`, `TOL`, `PREF`.
- [x] Находит в `Resist` столбцы `COMMENT`, `FOOTPRINT`, `TOL`, `PREF`, а также `COMMENT_FR`, `FOOTPRINT_FR`, `TOL_FR`.
- [x] Проверяет, что в `DataResist` есть строки для обработки, и переводит рабочие столбцы в текст.
- [x] Проходит строки `DataResist` со 2-й до последней.
- [x] Для каждой строки перебирает строки словаря `Resist` и считает совпадения по 4 полям.
- [x] При полном совпадении заменяет `COMMENT` и `FOOTPRINT` на значения из `_FR`-колонок словаря.
- [x] При 3/4 совпадении добавляет компонент в `NoMatch_Resistors` и подсвечивает совпавшие/не совпавшие поля.
- [x] Ведет статистику `Stats_Resistors_Total`, `Stats_Resistors_Full`, `Stats_Resistors_Partial`.
- [x] Закрывает словарь и завершает обработку; корректировка `ROTATION` идет уже отдельным макросом `CalculateRotationForResist`.

Задача 14 закрыта 16.08.2026 00:10.
Коммит создан: `a050da4` — `Align P&P resist matching`.

15. Работаем над перенесением логики макроса `CalculateRotationForResist`

- [x] Берет `Dict\\Dict.xlsx` из дерева `Dict/` как единственный источник словаря P&P — сделано.
- [~] Открывает лист `DataResist` в активной книге — частично: в коде есть `dataResistTable`, но это уже табличное состояние CSV-пайплайна, а не Excel-лист.
- [~] Проверяет, что файл словаря существует, и что в книге есть листы `DataResist` и `Resist` — частично: существование `Dict.xlsx` проверяется, `Resist` читается, а `DataResist` берется из текущего состояния.
- [x] Находит в `DataResist` столбцы `COMMENT`, `FOOTPRINT`, `ROTATION`, `LAYER` — сделано.
- [x] Находит в `Resist` столбцы `COMMENT_FR`, `FOOTPRINT_FR`, `ROTATION_DELTA` — сделано.
- [x] Проверяет наличие всех нужных столбцов и завершает шаг с сообщением, если чего-то не хватает — сделано.
- [x] Определяет последнюю строку по `COMMENT` в `DataResist` и по `COMMENT_FR` в словаре — сделано.
- [~] Отключает обновление экрана перед обработкой строк — частично: в JS-пайплайне отдельного Excel-экрана нет, но обработка идет без лишнего UI.
- [x] Проходит строки `DataResist` со 2-й до последней — сделано.
- [x] Берет текущие значения `COMMENT`, `FOOTPRINT`, `ROTATION`, `LAYER` — сделано.
- [x] Ищет точное совпадение по `COMMENT` и `FOOTPRINT` в листе `Resist` — сделано.
- [x] Если совпадение найдено, берет `ROTATION_DELTA` из словаря — сделано.
- [x] Для `TopLayer` считает `newRotation = (currentRotation - rotationDelta) Mod 360` и нормализует отрицательное значение — сделано.
- [x] Для `BottomLayer` считает угол по формуле `180 - currentRotation - rotationDelta` или `540 - currentRotation - rotationDelta`, а затем нормализует результат в диапазон `0..359` — сделано.
- [x] Если слой неизвестен, пропускает строку и увеличивает счетчик пропусков — сделано.
- [x] Записывает новый `ROTATION` в `DataResist` — сделано.
- [x] Ведет статистику обработанных и пропущенных строк — сделано.
- [~]Закрывает словарь и включает обновление экрана обратно — частично: в текущем коде это реализовано как завершение обработки и возврат результата без Excel-объектов.

Состояние на 16.08.2026: логика перенесена в код, но часть формулировок оставлена в терминах Excel-макроса для сравнения с оригиналом.
Задача 15 закрыта 16.08.2026 18:10.
коммит `c274726` — **Reclassify P&P rotation task 15**.- `f8c5cc5` — `Bump P&P version to 3.3.0 after task 15` - `65c9be0` — `Record task 15 status notes`

16. Работаем над перенесением логики макросов CopyDataResistToCapacitor, ProcessCapacitors, CalculateRotationForCapacitor, CopyDataCapacitorToOther

- [x] Берет `DataResist` как источник данных для следующего шага.
- [x] Проверяет, что `DataResist` существует.
- [~] Если `DataCapacitor` уже существует, решает, что делать: заменить его или завершить с сообщением. (сейчас таблица просто пересобирается из `DataResist`)
- [x] Создает копию `DataResist` и переименовывает ее в `DataCapacitor`.
- [x] После копирования запускает обработку конденсаторов по словарю `Dict.xlsx`.
- [x] Открывает лист `Capacitor` в словаре.
- [x] Находит в `DataCapacitor` столбцы `COMMENT`, `FOOTPRINT`, `PREF`.
- [x] Находит в `Capacitor` столбцы `COMMENT`, `FOOTPRINT`, `PREF`, `COMMENT_FR`, `FOOTPRINT_FR`.
- [x] Проверяет наличие всех нужных столбцов и завершает шаг с сообщением, если чего-то не хватает.
- [x] Определяет последнюю строку по `COMMENT` в `DataCapacitor` и в словаре.
- [~] Отключает обновление экрана перед обработкой строк. (в JS-цепочке нет Excel-экрана, но обработка идет без UI-мигания)
- [x] Проходит строки `DataCapacitor` со 2-й до последней.
- [x] Берет текущие значения `COMMENT`, `FOOTPRINT`, `PREF`.
- [x] Ищет точное совпадение по этим полям в листе `Capacitor`.
- [~] Если совпадение найдено, заменяет `COMMENT`, `FOOTPRINT`, `PREF` на значения из словаря. (в коде и макросе `PREF` участвует в поиске, а заменяются `COMMENT` и `FOOTPRINT`)
- [x] Подсвечивает измененные строки и ячейки.
- [x] Ведет статистику полного/частичного совпадения и строк без совпадения.
- [x] Ведет статистику обработанных и пропущенных строк.
- [x] Ведет счетчик `Rotation C` — у скольких элементов изменился угол — и добавляет его на панель после `Rotation R`.
- [~] Закрывает словарь и включает обновление экрана обратно. (закрытия Excel-объектов нет, но результат обработки уже возвращается в состояние)
- [x] Затем запускает `CalculateRotationForCapacitor`.
- [x] Берет `ROTATION` и `LAYER` из `DataCapacitor`.
- [x] Берет `ROTATION_DELTA` из листа `Capacitor`.
- [x] Для `TopLayer` считает `(currentRotation - rotationDelta) Mod 360`.
- [x] Для `BottomLayer` считает по формуле с `180` / `540` и нормализует результат.
- [x] Если слой неизвестен, пропускает строку.
- [x] Записывает новый `ROTATION` в `DataCapacitor`.
- [x] Ведет статистику обработанных и пропущенных строк.
- [x] Затем копирует `DataCapacitor` в `DataOther` через `CopyDataCapacitorToOther`.

Задача 16 закрыта 16.08.2026 19:10.
Коммит создан: `4a4f38a` — `Fix P&P preview sheet names`.

17. Работаем над перенесением логики макросов ProcessOther, CalculateRotationForOther, CopyDataOtherToDataPredExit

- [x] Проверяет, что `DataOther` существует. (лист уже формируется на этапе `CopyDataCapacitorToOther` и далее используется без изменения источника)
- [x] После этого запускает обработку `Other` по словарю `Dict.xlsx`. (обработка ветки `Other` добавлена в P&P-пайплайн)
- [x] Открывает лист `Other` в словаре. (словари читаются из `Dict.xlsx`, лист `Other` подключен)
- [x] Находит в `DataOther` столбцы `COMMENT`, `FOOTPRINT`, `ROTATION`, `LAYER`. (по этим колонкам идет матчинг и расчет углов)
- [x] Находит в `Other` столбцы `COMMENT_FR`, `FOOTPRINT_FR`, `ROTATION_DELTA`. (эти поля используются для замены и коррекции вращения)
- [x] Проверяет наличие всех нужных столбцов и завершает шаг с сообщением, если чего-то не хватает. (ошибка выбрасывается явно)
- [x] Определяет последнюю строку по `COMMENT` в `DataOther` и в словаре. (цикл работает по реальным данным, без пустых хвостов)
- [x] Отключает обновление экрана перед обработкой строк. (в JS это эквивалентно пакетной обработке без промежуточного рендера)
- [x] Проходит строки `DataOther` со 2-й до последней. (заголовок не трогаем, данные идут с первой компонентной строки)
- [x] Берет текущие значения `COMMENT`, `FOOTPRINT`, `ROTATION`, `LAYER`. (используются для сравнения и перерасчета)
- [x] Ищет точное совпадение по `COMMENT` и `FOOTPRINT` в листе `Other`. (сравнение идет по исходным значениям, как в макросе)
- [x] Если совпадение найдено, заменяет `COMMENT` и `FOOTPRINT` на значения из словаря. (подстановка `_FR`-версий после полного совпадения)
- [x] Подсвечивает измененные строки и ячейки. (полные совпадения помечаются стилем и цветом)
- [x] Ведет статистику полного/частичного совпадения и строк без совпадения. (сводка выводится в `otherStats`)
- [x] Ведет счетчик `Stats_RefMarks_Count` по строкам, где `DESIGNATOR` начинается с `REF`. (счетчик сохраняется для сводки)
- [x] Ведет статистику обработанных и пропущенных строк. (для контроля прогресса и отладки)
- [x] Затем запускает `CalculateRotationForOther`. (углы пересчитываются отдельным шагом)
- [x] Берет `ROTATION` и `LAYER` из `DataOther`. (исходный угол и слой нужны для формулы)
- [x] Берет `ROTATION_DELTA` из листа `Other`. (дельта приходит из словаря)
- [x] Для `TopLayer` считает `(currentRotation - rotationDelta) Mod 360`. (формула перенесена из макроса)
- [x] Для `BottomLayer` считает по формуле с `180` / `540` и нормализует результат. (нижний слой обрабатывается отдельно)
- [x] Если слой неизвестен, пропускает строку. (строка остается без изменения)
- [x] Записывает новый `ROTATION` в `DataOther`. (обновление идет в ту же таблицу)
- [x] Ведет статистику обработанных и пропущенных строк. (счетчики отражают итог расчета)
- [x] Ведет счетчик `Rotation O` — у скольких элементов `Other` пересчитались углы — и добавляет его на панель после `Rotation C`. (счетчик показан в P&P UI)
- [x] После завершения всех изменений в `DataOther` копирует его в `DataPredExit` через `CopyDataOtherToDataPredExit`. (финальный снимок ветки `Other`)
- [x] Если `DataPredExit` уже существует, решает, что делать: заменить его или завершить с сообщением. (на этом шаге формируется отдельная копия данных)
- [x] Создает копию `DataOther` и переименовывает ее в `DataPredExit`. (лист-приемник готовится перед экспортом)

Задача 17 закрыта 16.08.2026 20:14.
Коммит **`c1a17bd`** (`Finalize P&P other-stage tracking`).

18. Работаем над перенесением логики макроса `GenerateFileNameFromVariant` (в работе)

- [x] Берет исходный вариант из `Info!D5`, а если `D5` пустой — берет путь CSV из `Info!D6` и работает от имени файла. (это точка входа для вычисления имени)
- [x] Извлекает базовый код через `ExtractBaseCodeFromText`: ищет `НСФТ` и берет после него до 10 цифр/точек. (в имени это часть вида `080101.001`)
- [x] Формирует исполнение из `Info!D3` как `-0X`, если значение больше нуля, иначе оставляет пустым. (это отдельный префикс в имени)
- [x] Определяет суффикс слоя через `DetermineLayerSuffix` по листу `DataOther`: `Top` -> `_T_`, `Bottom` -> `_B_`, оба -> `_R_`, ничего -> пусто. (маркер берется из реального состава строк, а не из `Variant`)
- [x] Извлекает дату через `ExtractDateFromText`: берет последние 8 цифр из текста и сохраняет их как `YYYYMMDD`. (дата идет в конце имени в скобках)
- [x] Собирает предложенное имя по формуле макроса: `baseCode + dashPart + layerSuffix + "(" + datePart + ")"`. (именно эта строка показывается в `InputBox`)
- [x] Показывает `InputBox` для ручной правки имени файла. (пользователь может принять или изменить предложенный вариант)
- [x] Обрабатывает подтверждение или отмену без нарушения последовательности макроса. (при отмене поведение должно повторять VBA-логику)
- [x] Записывает итоговое имя в `Info!D7`. (после этого `D7` становится единственным источником имени для экспорта)
- [x] Показывает `Info!D7` в правой части заголовка превью P&P рядом с надписью `Таблица P&P`. (имя должно быть выровнено максимально вправо)
- [x] Передает управление следующим этапам экспорта без изменения P&P-ветки. (дальше уже идут `ProcessPickAndPlaceData`, `SaveAsTextWithSpaceDelimiterFinalFixedError3`, `SaveXLSXAndClose`)
- [x] Проверяет, что логика не трогает `DataPredExit`, `DataExit` и предыдущие стадии. (эта задача отвечает только за формирование имени файла)

Задача 18 закрыта 16.08.2026 20:41.
Готово. Версию поднял: **3.5.4** для приложения и **9.23.0** для UI.
Коммит: `ba8c577` — `Finalize P&P filename task`

19. Переносим логику макроса `ProcessPickAndPlaceData`

- [x] Работает по `DataPredExit`, а `DataOther` не трогает. (это входная таблица для финальной подготовки данных)
- [x] Создает `DataExit`, если его еще нет, и полностью очищает его перед заполнением. (лист должен начинаться с чистого состояния)
- [x] Сортирует таблицу на листе `DataPredExit` по `COMMENT` по возрастанию. (сортировка должна происходить до переноса в `DataExit`)
- [x] На листе `DataPredExit` переставляет столбцы в порядок: `DESIGNATOR`, `FOOTPRINT`, `CENTER-X(MM)`, `CENTER-Y(MM)`, `LAYER`, `ROTATION`, `COMMENT`. (именно этот порядок нужен для финального файла)
- [x] Копирует только видимые строки в `DataExit`, вставляя их с строки 3. (первая и вторая строки пока остаются служебными)
- [x] В `A1` пишет строку точно как она записана в `Version.md` в корне проекта. (версия должна браться из файла, а не собираться вручную)
- [x] В `A2` пишет имя файла из `Info!D7`; если `D7` пустой — берет имя книги.
- [x] Удаляет строку 3 на листе `DataExit` как служебную строку-заголовок после переноса. (после этого данные начинаются с строки 3)
- [x] Заменяет все `Fiducial` на `0` по всему `UsedRange`. (это финальная нормализация перед сохранением)
- [x] Сохраняет книгу. (после этого данные готовы к дальнейшему экспорту)

Задача 19 закрыта 16.08.2026 23:08
Коммит создан: `f8b8f6f` — `Refine DataExit preview`.
Версию поднял: **3.5.11** для приложения и **9.27.3** для UI.

20. Переносим логику макроса SaveAsTextWithSpaceDelimiterFinalFixedError3

- [x] Берёт имя файла из `Info!D7`. (это базовое имя для TXT-экспорта)
- [x] Если `D7` пустой — просит в `InputBox` ввести имя и записывает его обратно в `D7`. (пустое имя не должно проходить дальше)
- [x] Если расширения `.txt` нет — добавляет его. (расширение должно появляться автоматически, но в D7 не вписывай txt)
- [x] Берёт лист `DataExit`. (экспорт идет только из финального листа)
- [ ] Переводит расчёт в `Manual`. (в Electron-пути отдельный режим расчёта не используется)
- [x] Формирует текст для экспорта из 7 столбцов. (строго по структуре финального листа)
- [x] Строки 1–2 пишет как есть. (служебные строки не трогаем)
- [x] Строки с 3-й и ниже пишет через `Trim`. (убираем лишние пробелы при выгрузке)
- [x] Разделитель столбцов данных один пробел.
- [x] Пустые строки ниже 2-й не добавляет. (пустые записи не должны попадать в TXT)
- [x] Пишет TXT-файл по путям из дерева каталогов, если они заданы: `Экспорт P&P` и `Адрес расстановщика`. (экспорт в обе точки должен идти отдельно)
- [x] Если папки нет или запись не удалась — собирает текст ошибки. (ошибки должны отображаться в итоговом сообщении)
- [x] После сохранения TXT откладывает окно статистики по проекту до задачи 21. (сохранение статистики и лист `Stats` пойдут позже)

21. Сохраняем полную статистику в лист `Stats`

- [x] Собирает сводную статистику по резисторам, конденсаторам и прочим компонентам.
- [x] Выводит в окно краткую выжимку по итогам обработки, реперам и `Not Fitted`.
- [x] Сохраняет полный отчет на лист `Stats` в конец xlsx-файла.
- [x] Добавляет в `Stats` счетчики переименования, поворота, комментариев и удаления `Not Fitted`.
- [x] Показывает результат сохранения TXT и XLSX в итоговом окне.

Задача 20 закрыта 17.08.2026 00:02
Версию поднял до **3.5.11** / **9.27.3**.
Сделал коммит: `1e6264d` — `Add automatic P&P TXT export`.

22. Корректируем лист Stats

Давай по листу Stats сверху вниз пройдем.
1. [R] РЕЗИСТОРЫ:
Всего: 37
Полных совпадений (4/4): 12
Частичных (3/4): 0
Без совпадений: 25

По подсчету Всего, Полных совпадений, Частичных, Без совпадений. 
Пример: всего элементов в проекте 188 (с REF). REF в проекте 7. У нас есть счетчик REF, поэтому все данные удобно было бы считать уже без REF. Т.е. в проект у нас зашло 180 (это по Лист1: Количество строк- заголовок - 7 REF ). Это отдельным счетчиком (назови "Входимость элементов") сделай в Stats в блоке [=] ИТОГО:. 
Далее у нас идет отсев SET=0 на листе DataSet, про этот счетчик `Delete SET=0` ниже опишу. После отсева осталось 38 строк-заголовок-7REF= 30. Вот это наше "ВСЕГО"
Полных вроде правильно считаешь
Частичных вероятно тоже
Без совпадений должно быть так посчитано: Всего-полных-частичных (именно в этом блоке).

Все так же по логике в блоке [C] КОНДЕНСАТОРЫ: и [O] ПРОЧИЕ:

2. блок ИТОГО
Обработано: 111 - заменяем на `Входимость элементов` (логику подсчета описал ранее)
Всего: - повторить цифру из верхнего блока любого
Полных совпадений: 30 (27.0%) - тут верно - сумма полных, но не верно проценты. Проценты надо считать от `Всего`
Частичных: 0 (0.0%) - сумма частичных
Без совпадений: 81 (73.0%) - тут не верно. Формула должна быть такой: `Всего` - (Полные_R + частичные_R) - (Полные_C + частичные_C) - (Полные_O + частичные_O) Процент тут от `Всего` должен считаться

3. [*] В проекте 7 реперных знаков. - нормально, только нужна после пустая строка для визуального отделения

4. [*] Удалено Not Fitted: 150 компонентов. - переименуй в `Delete SET=0`, логика вероятно верная

5. [*] Удалено Not Fitted + SET=1 - этот счетчик у нас уже есть, подтяни сюда данные из него

6. === СТАТИСТИКА ПЕРЕИМЕНОВАНИЯ И ПОВОРОТА ===
Всего строк: 187 - это замени на `Входимость элементов` который у нас уже будет
С верхнего слоя: 113 - это переименуй в `Входимость компонентов TOP`. Подсчитывать надо так  `Входимость компонентов TOP`= компоненты TOP - REF_TOP. Брать на Лист1
С нижнего слоя: 74 - это переименуй в `Входимость компонентов BOT`. Подсчитывать надо так  `Входимость компонентов BOT`= компоненты BOT - REF_BOT. Брать на Лист1
Выход компонентов TOP - добавь и считать надо так `Выход компонентов TOP`= компоненты TOP - REF_TOP. Брать с листа DataPredExit
Выход компонентов BOT - добавь и считать надо так `Выход компонентов BOT`= компоненты BOT - REF_BOT. Брать с листа DataPredExit
Переименовано: 0 - убрать
Повернуто: 0 - нужно сравнить построчно лист DataSet2 и лист DataOther по значениям Rotation. Сколько углов изменилось записать тут.
С комментариями: 187 - убрать

7. === PNP_ROTATION ===
Resistor: Total 37 | Processed 12 | Skipped 25
Capacitor: Total 37 | Processed 8 | Skipped 29 | Changed 2
Other: Total 37 | Processed 10 | Skipped 27 | Changed 0

Меняем весь блок так:

=== Rotation ===
Resistor: - нужно сравнить построчно лист DataSet2 и лист DataResist по значениям Rotation. Сколько углов изменилось записать тут.
Capacitor: - нужно сравнить построчно лист DataResist и лист DataCapacitor по значениям Rotation. Сколько углов изменилось записать тут.
Other: - нужно сравнить построчно лист DataCapacitor и лист DataOther по значениям Rotation. Сколько углов изменилось записать тут.

8. === ЧАСТИЧНЫЕ И БЕЗ СОВПАДЕНИЙ ===
пока оставим

9. === РЕЗУЛЬТАТ СОХРАНЕНИЯ ФАЙЛОВ ===
пока оставим.

Задача 22 закрыта 17.08.2026 22:28
Версию поднял до **3.5.28** / **9.27.3**.
Коммит: **88687b7** — `Open stats in separate window`.

Была проблема с углами
кажется решилась в Версия поднята до **3.5.31**, коммит `65e0a83`.

Готово — файл `Compare TXT\txt_compare.html` (версия 1.0.9) добавлен в репозиторий и закоммичен в текущую ветку `notorigami77-purple-tiles-half-width` (коммит `8f941d2`).