# plan.md

**Описание:** Короткий рабочий план по текущему состоянию ASM Project Generator.  
**Версия:** 2.5.0
**Автор:** Новожилов Артем

## Что уже сделано

- Это HTML/CSS/JavaScript приложение на Electron.
- Electron-обвязка создана и запускается через `npm start`.
- Подключены `main.js` и `preload.js`.
- Работает чтение шаблонов и карты полей.
- Работает генерация `PR1`, `ISD`, `pxf`.
- Работает изменение бинарного `PR1` по полям формы.
- Работает запись AOI `.txt`.
- Стартована обработка `Создать проект АОИ` на основе P&P `.txt`.
- Папка `templates` целиком хранится в проекте, а `template.PR1` используется как дефолтный шаблон для заполнения полей.
- Snapshot `*_project_snapshot.json` сохраняется в локальную папку и дублируется в `%AppData%`.
- Сделаны верхние карточки, раздельный скролл и тёмный UI.
- Добавлены скорости печати переднего и заднего ракеля.
- Добавлены настройки и всплывающие подсказки с задержкой.
- Добавлен блок обновлений программы с проверкой через GitHub Releases.
- Настройки и служебные данные пользователя перенесены в AppData.

## Что в работе

1. Доводить UX без ломки dev-режима.
2. Следить, чтобы новые поля попадали и в snapshot, и в PR1 patching.
3. Поддерживать набор подсказок и обновлений в актуальном состоянии.

## Ближайшие шаги

1. Проверять реальные правки формы на сохранение и генерацию.
2. Добавлять новые подсказки сразу при появлении новых полей.
3. Подготовить публикацию релиза на GitHub Releases.

## Последние рабочие файлы

- `asm_generator_form_v9.html`
- `main.js`
- `preload.js`
- `package.json`
- `PROJECT_CONTEXT.md`

## Текущая версия проекта

- приложение: `2.5.0`
- дата версии: `2026-08-05`

## Контрольная точка

- локальные стили, шрифты и иконки собраны в `assets`, поэтому интерфейс не зависит от доступа к интернету
- текущая версия включает UI обновлений, GitHub Releases и AppData-хранилище
- с версии `2.5.0` стартовала обработка `Создать проект АОИ` на основе P&P `.txt`
